import { composeFactory } from '@meetings-scheduler/drizzle-fixtures';

// Add your fixture files here as you define new tables, e.g.:
// import { userFixture } from './users';

export const factory = composeFactory({
	// users: userFixture,
});
