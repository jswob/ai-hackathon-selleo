import { t } from 'elysia';

// Add reusable TypeBox validation schemas here. Example:
// export const labelColor = t.Union([
// 	t.Literal('red'),
// 	t.Literal('blue'),
// ]);

// Placeholder to keep the file importable
export const _schemas = t.Object({});
